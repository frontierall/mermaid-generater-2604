# Docs

이 폴더는 `mermaid-generater-2604` 프로젝트에서 확인된 이슈, 원인, 해결 방식, 재발 방지 포인트를 정리하기 위한 문서 모음입니다.

현재 기준으로 남겨진 세션 연속 기록 파일은 다음입니다.

- `2026-04-07-175450-this-session-is-being-continued-from-a-previous-c.txt`

다만 위 파일에는 상세 이슈 로그가 충분히 남아 있지 않았습니다. 그래서 아래 문서는 다음 근거를 함께 사용해 작성했습니다.

- 세션 연속 기록 파일
- 현재 코드베이스에 반영된 방어 로직
- `app.js` 내부 주석에 남아 있는 원인/해결 메모

문서 목록:

- `issue-history.md`: 지금까지 코드에 반영된 주요 문제와 해결 내용의 종합 정리

권장 사용 방식:

1. 새로운 버그를 수정하면 `issue-history.md`에 같은 형식으로 추가합니다.
2. 재현 조건이 있으면 반드시 입력 예시와 실패 증상을 함께 남깁니다.
3. AI 프롬프트 수정만으로 해결한 경우와 코드 정규화로 해결한 경우를 구분해 적습니다.
